[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "weather-vibes"
version = "0.1.0"
description = "Analytics-ready Michigan GHCN-Daily weather data"
requires-python = ">=3.11"
dependencies = ["duckdb>=1.1,<2"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
weather-vibes = "weather_vibes.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

